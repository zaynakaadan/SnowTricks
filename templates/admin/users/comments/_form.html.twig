
{{ form_start(commentForm)}}
    {{ form_row(commentForm.content) }}        
       

    {{ form_row(commentForm.trick) }}
{{ form_end(commentForm) }}
<button type="submit" class="btn btn-primary">Enregistrer</button>
